# What the pipeline has taught us

Append-only, newest at the bottom. One entry per prospecting round. The
point is that round six should be better at this than round one — which only
happens if round one wrote down what it noticed.

Worth recording:

- **Which source produced qualified leads**, and how many per hour of work.
  Reorder `sources.md` when the ranking changes, and say here that you did.
- **Which signal turned out to be the tell.** The rubric's weights are a
  starting hypothesis. If firms with `manual_template_evidence` keep
  converting and `conference_attendee` keeps going nowhere, that is a case
  for reweighting — raise it rather than quietly working around it.
- **Search phrasing that surfaced firms the obvious queries missed.**
- **Dead ends**, so nobody works them twice.

---

_No rounds recorded yet._
